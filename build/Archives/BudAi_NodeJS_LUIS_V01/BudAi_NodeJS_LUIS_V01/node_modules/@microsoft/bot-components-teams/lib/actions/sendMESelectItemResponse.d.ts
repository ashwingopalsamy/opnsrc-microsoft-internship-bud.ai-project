import { BoolExpression } from 'adaptive-expressions';
import { Activity } from 'botbuilder';
import { Converter, ConverterFactory, DialogConfiguration, DialogContext, DialogStateManager, DialogTurnResult, TemplateInterface } from 'botbuilder-dialogs';
import { BaseTeamsCacheInfoResponseDialog } from './baseTeamsCacheInfoResponseDialog';
export interface SendMESelectItemResponseConfiguration extends DialogConfiguration {
    disabled?: boolean | string | BoolExpression;
    card?: TemplateInterface<Activity, DialogStateManager>;
}
/**
 * Send a messaging extension response when an item is selected.
 */
export declare class SendMESelectItemResponse extends BaseTeamsCacheInfoResponseDialog implements SendMESelectItemResponseConfiguration {
    /**
     * Class identifier.
     */
    static $kind: string;
    /**
     * Gets or sets template for the attachment template of a Thumbnail or Hero Card to send.
     */
    card?: TemplateInterface<Activity, DialogStateManager>;
    getConverter(property: keyof SendMESelectItemResponseConfiguration): Converter | ConverterFactory;
    /**
     * Called when the dialog is started and pushed onto the dialog stack.
     *
     * @param {DialogContext} dc The [DialogContext](xref:botbuilder-dialogs.DialogContext) for the current turn of conversation.
     * @param {object} _options Optional, initial information to pass to the dialog.
     * @returns {Promise<DialogTurnResult>} A promise representing the asynchronous operation.
     */
    beginDialog(dc: DialogContext, _options?: Record<string, unknown>): Promise<DialogTurnResult>;
    /**
     * Builds the compute Id for the dialog.
     *
     * @returns {string} A string representing the compute Id.
     */
    protected onComputeId(): string;
}
