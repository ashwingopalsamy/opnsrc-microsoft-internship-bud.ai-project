import { BoolExpression, Expression, StringExpression } from 'adaptive-expressions';
import { Converter, ConverterFactory, Dialog, DialogConfiguration, DialogContext, DialogTurnResult } from 'botbuilder-dialogs';
export interface GetTeamMemberConfiguration extends DialogConfiguration {
    disabled?: boolean | string | BoolExpression;
    property?: string | Expression | StringExpression;
    memberId?: string | Expression | StringExpression;
    teamId?: string | Expression | StringExpression;
}
/**
 * Calls TeamsInfo.GetTeamMember to retrieve the list of members in a teams
 * scoped conversation and sets the result to a memory property.
 */
export declare class GetTeamMember extends Dialog implements GetTeamMemberConfiguration {
    /**
     * Class identifier.
     */
    static $kind: string;
    /**
     * Gets or sets an optional expression which if is true will disable this action.
     *
     * @example
     * "user.age > 18".
     */
    disabled?: BoolExpression;
    /**
     * Gets or sets property path to put the value in.
     */
    property?: StringExpression;
    /**
     * Gets or sets the expression to get the value to use for member id.
     *
     * @default
     * =turn.activity.from.id
     */
    memberId: StringExpression;
    /**
     * Gets or sets the expression to get the value to use for team id.
     *
     * @default
     * =turn.activity.channelData.team.id
     */
    teamId: StringExpression;
    getConverter(property: keyof GetTeamMemberConfiguration): Converter | ConverterFactory;
    /**
     * Called when the dialog is started and pushed onto the dialog stack.
     *
     * @param {DialogContext} dc The [DialogContext](xref:botbuilder-dialogs.DialogContext) for the current turn of conversation.
     * @param {object} _options Optional, initial information to pass to the dialog.
     * @returns {Promise<DialogTurnResult>} A promise representing the asynchronous operation.
     */
    beginDialog(dc: DialogContext, _options?: Record<string, unknown>): Promise<DialogTurnResult>;
    /**
     * Builds the compute Id for the dialog.
     *
     * @returns {string} A string representing the compute Id.
     */
    protected onComputeId(): string;
}
