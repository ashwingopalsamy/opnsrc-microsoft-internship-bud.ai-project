import { Activity, CardAction } from 'botbuilder';
import { DialogContext } from 'botbuilder-dialogs';
import { BaseAuthResponseDialog, BaseAuthResponseDialogConfiguration } from './baseAuthResponseDialog';
/**
 * Send a messaging extension 'result' response when a Teams Invoke Activity is received with activity.name='composeExtension/queryLink'.
 */
export declare class SendMEAuthResponse extends BaseAuthResponseDialog implements BaseAuthResponseDialogConfiguration {
    /**
     * Class identifier.
     */
    static $kind: string;
    /**
     * Builds the compute Id for the dialog.
     *
     * @returns {string} A string representing the compute Id.
     */
    protected onComputeId(): string;
    protected createOAuthInvokeResponseActivityFromCardAction(dc: DialogContext, cardAction: CardAction): Partial<Activity>;
}
